{:paths ["src" "test"]
 :tasks
 {test {:doc "Run seigyo cljc actor boundary tests."
        :task (do
                (require 'clojure.test
                         'seigyo.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'seigyo.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
